# ---------------------------------------------------------------------------
# NOTE: Install PyTorch for ROCm (MI300X) BEFORE pip-installing this file.
#
#   DigitalOcean MI300X droplet ships ROCm 7.0/7.1.
#   Use the matching PyTorch wheel:
#
#   If using the PyTorch Quick Start image (recommended):
#     PyTorch 2.6.0 is already installed — skip this step entirely.
#     Just run:  pip install -r requirements.txt && pip install -e .
#
#   If using the bare ROCm image:
#     pip install torch torchvision torchaudio \
#         --index-url https://download.pytorch.org/whl/rocm6.3
#
#   Optionally install FlashAttention for ROCm (compiled wheel from AMD):
#     pip install flash-attn  # only if a ROCm-compatible wheel is available;
#                             # code falls back to PyTorch SDPA if not found.
# ---------------------------------------------------------------------------
torch>=2.1.0
torchac>=0.9.3
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0
zstandard>=0.21.0
tensorboard>=2.14.0
einops>=0.7.0
pytest>=7.0
h5py>=3.9.0
